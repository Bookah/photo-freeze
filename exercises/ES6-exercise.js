// Write a function using ES5(!) that counts down from a start value until 0 (hint: setTimeout()). 
// It should have one mandatory parameter that tells it where to start
// And a second optional parameter telling the interval between counts (default 1000 milliseconds)

//count(10)
//count(10, 500)

// Now rewrite the function using ES6

// Check it at https://lebab.io/try-it